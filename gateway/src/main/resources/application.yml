spring:
  cloud:
    gateway:
      routes:
        - id: product-service
          uri: lb://product-service
          predicates:
            - Path=/product/api/**
          filters:
            - RewritePath=/product/(?<segment>.*), /$\{segment}